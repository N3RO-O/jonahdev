# TODO

- [ ] Diagnose mobile responsiveness issue (likely Navbar mobile overlay/menu)
- [ ] Patch `src/components/Navbar.jsx` to prevent body scroll getting stuck and ensure mobile overlay closes reliably
- [ ] Add viewport-change guard (reset `open` when switching breakpoints)
- [ ] Run `npm test` / `npm run build` (or dev build) to ensure no regressions

